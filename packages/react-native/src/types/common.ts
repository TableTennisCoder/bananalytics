/** Generic properties map for event data. */
export type Properties = Record<string, unknown>;

/** ISO 8601 timestamp string. */
export type Timestamp = string;

/** UUID v4 string identifier. */
export type UUID = string;
